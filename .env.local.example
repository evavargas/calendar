# Local without credentials
#
# Two modes:
#
# A) Front-only mock (no Java):
#   VITE_USE_MOCK_API=true
#
# B) Front + Java API (H2 + mock Google login/calendar):
VITE_USE_MOCK_API=false
VITE_API_BASE_URL=
